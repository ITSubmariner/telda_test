drop table if exists `regions`;

create table `regions` (
     id int not null generated by default as identity (start with 1),
     name varchar(50) not null unique ,
     title varchar(10) not null unique,
     primary key (id)
);
insert into `regions` (name, title) values ('Moscow', 'MSK' );
insert into `regions` (name, title) values ('Saint-Petersburg', 'SPB');
